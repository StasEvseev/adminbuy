language: python
python:
  - "2.7"

services:
  - postgresql

before_script:
  - psql -c 'create role test with login superuser password \'test\';' -U postgres
  - psql -c 'create database buyapi with owner test;' -U postgres

install:
  - pip install -r REQUIREMENTS

script:
 - python -m unittest discover